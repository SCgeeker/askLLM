# v1.1 E2E Acceptance Record (W5-B)

Date: 2026-07-23 | Environment: jamovi 2.7.37 / askLLM 1.1.0 (sideloaded `dist/askLLM_1.1.0_win64_jamovi-2.7.jmo`) | Raw screens: `askLLM_W5A_test.html` (user export)

| Check | Result | Evidence |
|-------|--------|----------|
| A Path truthfulness | ✅ | iris question: all 7 cited paths real and **quoted verbatim in catalog format**, including three-level structures (`Analyses > ANOVA > Non-Parametric > One-Way ANOVA — Kruskal-Wallis`, `… > Logistic Regression > N Outcomes — Multinomial`, `… > Factor > Data Reduction > Principal Component Analysis`) |
| B Off-list behaviour | ✅ | Mediation question: states plainly no dedicated tool is installed, suggests the installed Rj (real path) with lavaan, and names medmod/PATHJ from the official list with the Modules > jamovi library route — zero fabrication |
| C Degradation | ✅ | With includeCatalog off, behaviour reverts to v1.0's loose style (➔ arrows, non-verbatim menu names such as "Multinomial Logistic Regression") |
| D Privacy sentence | ✅ | **Blank-question guidance state (no LLM call)**: with the question empty the gate condition holds and the guidance text is shown, containing the spec §9.3 bilingual privacy sentence (HTML block 4) |
| E Detection correctness | ✅ | **Programmatic check (not a blank-question submit)**: `scan_modules()` run in an R console vs disk — all 9 analysis-bearing modules detected (jmv 26 + scatr 3 + GAMLj3 4 + MAJOR 6 + Rj 2 + askLLM 1 + jpower 4 + pamlj 8 + semlj 2 = 56 analyses); the only disk-vs-scan difference is lsj-data / r-datasets, both data-only (analyses = 0), filtered by design per spec S2; errors = 0. **In-app corroboration**: A/B (real calls with a question + submit) show the scan result actually reached the prompt — A quotes installed-module paths verbatim, B recognizes the installed Rj and lists medmod as installable |

**Methodology note (answering whether D & E were "blank submit")**:
- **D** is observable in the blank-question guidance state and **triggers no scan or LLM call** (an empty question is stopped at the gate).
- **E** cannot be verified by a blank submit — an empty question never scans; E is a separate programmatic full census in an R console, corroborated end-to-end by the real UI calls in A/B.
- A/B/C are all **real calls with a question + submit**; C is the includeCatalog-off degradation control.

Together with the W5-A quantitative result (`catalog-hit-rate.md`: v1.1 18/18 = 100% verbatim menu-path hits vs structural fabrication in v1.0), scenario S5b is accepted.

## Re-examination fixes (1.1.1, 2026-07-24)

Re-reading the raw screens (`askLLM_W5A_test.html`) corrected two earlier over-optimistic judgements, both now fixed:

1. **The caveat lied in degradation mode**: `.askllm_caveat_text()` was a fixed string claiming "menu paths checked against your installed modules" regardless of whether the list was attached — but check C (includeCatalog off) sent no list. Now `.askllm_caveat_text(has_catalog)`: when no list is attached it says plainly that paths were NOT verified. (test-brun.R)
2. **Check B had one grounding leak**: the mediation answer named "medmod or **PATHJ**" — medmod is in the list (correct), PATHJ is not; the model pulled a real off-list module name from training memory. The earlier "zero fabrication" note was wrong. Root cause: "NEVER invent module names" doesn't work (the model doesn't consider citing a real module to be inventing). Reworded to a membership constraint: "Name ONLY modules that appear literally in <available_modules>." Live re-test (gemini + gpt-4o-mini): the PATHJ leak is gone; a remaining `lavaan` mention is a legitimate "write R code using the lavaan package in Rj" suggestion, not a module leak.

Residual limitation (recorded honestly): module-NAME grounding is now much tighter, but the LLM may still mention R packages (lavaan, psych) when proposing an Rj-based workaround — a legitimate reference, not a fabrication.

Note: scanning produced 5 harmless yaml warnings (`NAs introduced by coercion: . is not a real`, from `.` values in some modules' yaml); inside the engine these are silently collected by withCallingHandlers and have no functional effect. Could be suppressed at the read layer later if desired.
