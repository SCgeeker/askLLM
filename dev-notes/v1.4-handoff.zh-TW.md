# v1.4 開發交接（cloud → local peer）

> cloud session 無法直接傳訊給本機 session（平台限制），本檔即回報管道：每次 push 更新一節，
> 本機驗證完把結果寫回本檔對應節的「本機驗證」欄再 push，cloud 端下次 fetch 即看到。
> 作者指示（2026-09-25）：接受方案 (a)；預設供應商改 Google Gemini flash；先做 F（CI + golden set）與 C（送出前預覽 + PII）。

> **本機離線驗證回報（peer，2026-09-25；R 4.6.1／jamovi 28.3.0.0）：**
> 推送 1、3 離線全綠（.h.R 已重生並提交）；**推送 2 有阻斷性 bug** —— `.askllm_extract_paths()` 未切句號「. 」／「 Or 」，test-eval-score.R 6 紅（詳見推送 2 欄），請先修再進 CI。
> GUI（作者，2026-09-25，已裝 1.4.0 於 jamovi 28.3）：推送 1 的 GUI 3/4/5 全 ✓（預設 Gemini、GitHub 隱藏、舊 .omv 退役、Gemini Submit）；推送 3 預覽面板 ✓；**PII 旗標通過** ✓（identifier-like 欄值 withheld、雙語旗標、門檻正確）。一個待釐清 GUI toast「Could not assign data / Sepal.Length」經查非 askLLM 程式所致（無寫欄路徑），詳見推送 3 欄。
> golden 基線待金鑰。module-catalog 2 個既有紅燈（fixture 未 commit，與 v1.4 無關）詳見推送 1 欄。

## 通用規則
- `.h.R` 一律由 `jmvtools::prepare()`／`install()` 重生（S2），cloud 端不手改；每次 a.yaml 有變動時本機都要重生。
- cloud 環境無 R，testthat 只能由本機跑；cloud 端每項都附「預期會動到的測試」。

---

## 推送 1：v1.4-0 預設供應商 + GitHub 選項 UI 隱藏 + 版本 1.4.0

**改動**
- `jamovi/askllm.a.yaml`、`askllmr.a.yaml`：`provider` default `nim` → `gemini`；`model` default → `gemini-flash-latest`（常青別名，與 `provider_spec('gemini')$default_model` 一致）；版本 `1.4.0`。`DESCRIPTION`、`0000.yaml` 同步 1.4.0。
- 方案 (a)：`jamovi/js/askllm.js`、`askllmr.js` 新增 `hideRetiredProviders(ui)`，`loaded` 事件呼叫：目前值不是 `github` 時把它從 Provider 下拉移除（先試 ComboBox `options` property，失敗退回 DOM 移除 `<option>`）；a.yaml enum 值保留，舊 `.omv` 載入時值為 github → 保留該項並由 R 端顯示退役說明。
- `docs/choose-model.html` 兩句「先從 NVIDIA 或 Gemini 開始」改為「Gemini 是預設」。
- 測試：`test-providers.R` 新增「a.yaml 預設 = gemini／gemini-flash-latest」與「js 具備 hideRetiredProviders 並於 loaded 呼叫」。

**本機驗證清單**
1. `jmvtools::prepare()` 重生兩個 `.h.R`（預期差異：`default="gemini"`、model default、`version = c(1,4,0)`）。
2. `devtools::test()`：預期全綠（既有 module-catalog fixture 紅燈除外——請在下方寫明該測試名稱與原因，cloud 端要據此設計 CI）。
3. GUI：新開分析 Provider 預設為 Google Gemini、Model 為 `gemini-flash-latest`；Provider 下拉**看不到** GitHub Models。
4. GUI：開一個 1.3.x 存的 `.omv`（provider=github）→ 下拉仍顯示「GitHub Models (retired 2026-07)」、結果面板顯示退役說明、不紅字。
5. GUI：Gemini 金鑰下 Submit 一問成功（確認 `gemini-flash-latest` 別名仍解析）。

**本機驗證（peer 填寫）** — 2026-09-25, R 4.6.1 / jamovi 28.3.0.0
- [x] 1 `jmvtools::prepare()` 重生兩個 `.h.R`：`default="gemini"`、`model="gemini-flash-latest"`、`version = c(1,4,0)`、`previewPayload` 就位 ✓（已提交）
- [x] 2 `test-providers.R` 新案例（a.yaml 預設 gemini／gemini-flash-latest、js 具 `hideRetiredProviders` 並於 loaded 呼叫）全綠 ✓；全套除下列外皆綠（test-eval-score 6 紅見推送 2、module-catalog 2 既有紅見下）
- [x] 3 GUI：新開分析 Provider 預設 Google Gemini、Model gemini-flash-latest、下拉看不到 GitHub Models ✓（作者確認 2026-09-25）
- [x] 4 GUI：1.3.x 舊 .omv（provider=github）→ 仍顯示「GitHub Models (retired 2026-07)」、退役說明、不紅字 ✓
- [x] 5 GUI：Gemini 金鑰下 Submit 一問成功、gemini-flash-latest 別名解析 ✓
- module-catalog 紅燈測試：
  - `test-module-catalog.R:176` `test_that('default dirs resolve relative env paths')` — `normalizePath(file.path(env_home,'rel'), mustWork=TRUE)` 找不到路徑；fixture `tests/testthat/fixtures/modules/env-home/rel` 不在 repo
  - `test-module-catalog.R:201` `test_that('self-location derives modules root')` — `normalizePath(file.path(root,'modules'), mustWork=TRUE)`；fixture `tests/testthat/fixtures/modules/self-location/modules/…` 不在 repo
  - 原因：兩組 fixture 樹未 commit（`git ls-files fixtures/modules/` 僅 a/ b/ broken/ rj-a/ known-modules-abc.yaml），**既有問題、與 v1.4 無關**（1.3.x 起即紅）。CI 建議：commit 缺的 fixture，或這兩個 test 在 fixture 缺席時 `skip_if(!dir.exists(...))`

---

## 推送 2：v1.4-F CI + golden set + 評分純函式

**改動**
- `R/eval-score.R`（新）：`.askllm_legal_paths()`／`.askllm_extract_paths()`／`.askllm_check_path_hits()` 自 tools/compare-models.R 搬進套件；新增 `.askllm_kw_hit()`、`.askllm_score_answer(text, expect, legal_paths, analysis)`（分項：keywords／forbidden_free／redirect／paths／code，總分 0–100 或 NA）、`.askllm_load_golden(path)`（schema 驗證、不 stop）、`.askllm_golden_dataset(name)`（只允許 `datasets::`）。
- `tools/golden/golden-set.yaml`（新）：13 題（Guider 9、R tutor 4；含雙向 redirect 各 1）。
- `tools/run-golden.R`（新）：`run_golden(provider, model, golden, analysis, ids, with_catalog, ...)` → `dev-notes/golden-<provider>-<model>-<stamp>.md/.json`（已加入 .gitignore）。
- `tools/compare-models.R`：三個 helper 改為轉接套件版本（行為不變）。
- `.github/workflows/test.yml`：push/PR 跑 `testthat::test_local(stop_on_failure=TRUE)`（ubuntu、R release、rspm）；`eval.yml`：workflow_dispatch 跑 golden（secrets：GEMINI_API_KEY／OPENROUTER_API_KEY／NVIDIA_API_KEY），報告為 artifact。
- `tests/testthat/test-eval-score.R`（新，離線）：路徑抽取／合法路徑／命中、關鍵字、評分各情境、golden schema 正反例、資料集解析。
- `.Rbuildignore` 加 `^\.github$`。

**本機驗證清單**
1. `devtools::test()`：新檔 test-eval-score.R 預期全綠；既有測試不受影響。
2. `pkgload::load_all(); source('tools/run-golden.R'); run_golden(provider='gemini')`：13 題跑完、產出 md/json；**請把 mean score 與各題 warnings 貼回本節**（這是 v1.4 的基線分數，之後 D／路徑圖改 prompt 都要對照它）。本機掃得到 jamovi 模組時路徑分項才計分，請在 jamovi 安裝目錄可見的環境跑（或傳 `scan_dirs=`，同 compare-models 註解）。
3. `source('tools/compare-models.R'); compare_models(models='gemini-flash-latest', provider='gemini')` 仍可跑（helper 轉接無誤）。
4. **CI**：push 到 GitHub 後看 Actions「tests」是否綠。若紅在 module-catalog fixture 測試，請把該測試名與原因寫在推送 1 的欄位；cloud 端下一輪處理（修 fixture 或加 skip 條件）。
5. （選配）在 repo Settings → Secrets 加 `GEMINI_API_KEY` 後手動跑 Actions「golden-eval」一次，確認 artifact 產出。

**本機驗證（peer 填寫）** — 2026-09-25
- [x] 1 **已由 peer 修復**（2026-09-25，commit 見下）：`.askllm_extract_paths()` 抽取前先切子句——`strsplit` 以 `\.\s+|\.$|;\s+|\s+[Oo]r\s+|，|。|；|、|或` 斷句，每句再抓一條 `Analyses > …`，解決「同行多路徑」與「. Or 未斷」。`test-eval-score.R` 全綠(80/80)、全套僅餘既有 module-catalog 2 紅。
  〔原始 bug 記錄〕regex `'Analyses\s*>\s*[^\n]+'` + `regexpr`(每行只取第一個)貪婪吃到行尾，`"A. Or B"` 被當成一條(`total=1` 應=2)，連帶 score_answer 全命中案例 66.7、paths 分項 0、warnings 多 1。
- [x] 2 golden set **基線已跑**（2026-09-25，gemini-flash-latest，key=環境變數 GOOGLE_API_KEY，Machine 層級）：13/13 ok、0 failed、**mean score = 96.2**、全部有計分（path 分項正常，0 NA）。12/13 得 100；唯一 50 分 `guider-warpbreaks-contingency`（warnings：`missing: chi|contingency|independence` —— 模型答題未用預期關鍵字，非 bug，此為該題基線）。報告 `dev-notes/golden-gemini-gemini-flash-latest-20260925-215520.{md,json}`（.gitignore，未入版控）。**⇒ v1.4 基線 = 96.2**，之後 D／路徑圖改 prompt 都對照它。
- [~] 3 `compare-models.R` 轉接：`load_all()` 無錯、三個 helper 已轉接套件版本（結構 OK）；真模型跑需金鑰，暫緩
- [ ] 4 CI：待 push 後於 GitHub Actions 觀察。注意 `test_local(stop_on_failure=TRUE)` 會因上述 test-eval-score 6 紅 + module-catalog 2 紅而失敗，需先修 eval-score 與處理 fixture
- [ ] 5 golden-eval action：待 secret，選配

---

## 推送 3：v1.4-C「Show what will be sent」預覽 + 高基數／PII 防護

**改動**
- `R/data-summary.R`：識別碼型守門 `.askllm_identifier_like(k, n)`（k > 20 且 k/n > 0.9）——character／factor 欄符合時只回報「k distinct (identifier-like; values withheld)」，不列任何值（現況缺口：`Name` 欄會把前 10 個人名送進 prompt）。新增 `.askllm_pii_flags(df, vars)`：欄名整詞／分節規則（id、name、email、phone、address、姓名、學號……）+ 值樣式（email／電話 ≥ 50%）+ 識別碼基數，回傳 `var (reason)` 字串向量。
- 兩個 a.yaml 新增 `previewPayload` Bool（title「Show what will be sent (no LLM call)」，default false）；u.yaml 在 testConnection 前加 CheckBox。
- `askllm.b.R`／`askllmr.b.R`：預覽旗標不受 submit 守門限制；摘要／catalog／rj／自訂 prompt 照常組裝，在 payload 組好、**快取比對之前**提前 return：answer（或 code）面板顯示 `.askllm_preview_text(system, user)` 原文，meta 顯示「preview only · 0 API calls」，instructions 顯示零呼叫聲明 + 字元數 + PII 提示。不動 state、不載金鑰、不呼叫。
- `.askllm_caveat_text(has_catalog, pii_flags=NULL)`、`.askllmr_caveat_text(has_rj_env, pii_flags=NULL)`：有旗標時附雙語「疑似個資」區塊；NULL/空時逐字不變（既有測試鎖）。成功回覆後兩分析都會帶 PII 提示。
- guide 文字兩分析各加一句預覽說明；README 兩語版 Quick start 與 Privacy 各加段落。
- 測試：`tests/testthat/test-pii.R`（新，離線）：守門邊界、字元／factor／ordered 不列值、多水準類別不受影響、iris 回歸鎖、PII 欄名／值樣式／基數、iris/mtcars 零旗標、預覽文字、caveat 降級保證、a/u.yaml 選項、`.runInner` 原始碼掃描（預覽分支在快取比對前且不呼叫 ask_llm）。

**本機驗證清單**
1. `jmvtools::prepare()` 重生 `.h.R`（新增 previewPayload）；`devtools::test()` 預期全綠。
2. GUI（兩分析各一次）：勾「Show what will be sent」但**不勾 Submit** → 面板顯示 system/user prompt 原文、meta「preview only · 0 API calls」；供應商後台確認零用量。
3. GUI：預覽 + Submit 同時勾 → 仍是預覽（不送）；取消預覽 → 正常送出；再勾預覽 → 之前的回覆不被清掉（預覽不動 state），取消預覽後 cached 回放。
4. GUI：資料含 `Name` 欄（每列不同）→ 預覽中該欄顯示「values withheld」且 instructions 出現「Possible personal data」旗標；Submit 後 caveat 也帶旗標。
5. 對照 golden 基線：`run_golden(provider='gemini')` 分數不應因本推送改變（prompt 內容未變，只有識別碼欄摘要格式在含 ID 欄時不同；golden 資料集皆無識別碼欄）。

**本機驗證（peer 填寫）** — 2026-09-25
- [x] 1 `jmvtools::prepare()` 重生 `.h.R`（`previewPayload` 就位，已提交）+ `test-pii.R` 全綠 ✓（守門邊界／character・factor・ordered 不列值／多水準類別不受影響／iris 回歸鎖／PII 欄名・值樣式・基數／iris・mtcars 零旗標／預覽文字／caveat 降級／a・u.yaml 選項／`.runInner` 原始碼掃描 皆過）
- [x] 2 GUI（iris）：勾「Show what will be sent」不勾 Submit → 面板正確顯示 system/user prompt 原文、meta「PREVIEW ONLY … prompt ≈ 4911 chars」✓（作者確認 2026-09-25）
- [~] 3 GUI：勾 preview 呈現預覽（作者確認）；預覽↔提交完整切換序列（預覽+提交仍預覽／取消後送出／再勾不清 cached）尚未逐項確認
- [x] 4 GUI PII：**通過** ✓(作者以 peer 備的 pii-test.csv 驗,2026-09-25)。摘要中 `student_id`／`email`／`Name` 皆顯示「30 distinct (identifier-like; values withheld)」不列值；Instructions 出現雙語「Possible personal data」旗標,三欄理由正確(email 另標 email pattern);`age`（13 levels、k/n=0.43）與 `score`／`group` 未誤標 → 門檻(k>20 且 k/n>0.9)正確。
- [ ] 5 golden 基線對照：待金鑰（同推送 2-2）

  ⚠ **GUI 觀察(待釐清,非阻斷)**：iris 預覽當下 jamovi 右下出現紅色 toast「Could not assign data — Cannot assign non-numeric value to column 'Sepal.Length'」。**經查 askLLM 1.4.0 執行碼無任何寫欄路徑**(a.yaml/h.R 無 Output 選項、r.yaml 全 Preformatted/Html、`.runInner` 不呼叫 setValues/fill_output、data-summary 只讀不寫 `self$data`),故此 toast **不來自 askLLM 程式**,研判為 jamovi 資料表層級訊息(貼上/匯入/computed 欄/重開 .omv)。待作者提供重現條件(是否僅 askLLM 預覽時出現、換乾淨 iris 是否重現)再定案。

---

## cloud 端待辦（依 peer 回報後）

> **作者決定（2026-09-25，方案 A）**：v1.4 功能已全數本機驗證通過（推送 1/3 GUI ✓、PII ✓、預設 Gemini ✓、舊 .omv 退役 ✓、eval-score 已修、golden 基線 96.2）。**唯一阻礙是 module-catalog 2 個 fixture 紅** → **請 cloud 先修 fixture／加 skip 讓 CI 轉綠，peer 再把分支 fast-forward 併入 main。** 在 CI 綠之前不併 main。

- **【已處理·cloud，2026-09-25】** module-catalog fixture 紅燈：補齊四組空目錄 fixture（`env-home/rel`、`appdata-root/jamovi/modules`、`libpath-root/modules/x/R`、`self-location/modules/askLLM/R/askLLM`，各放 `.gitkeep`），並把 S10 的 `APPDATA` 斷言限定 Windows（`.platform_dirs()` 在 Linux/macOS 不讀 APPDATA，否則 ubuntu CI 必紅）。CI workflow 加 `LANG/LC_ALL=C.UTF-8`（C locale 下 R 連含中文識別字的測試檔都解析不了，cloud 容器實測）。
  **cloud 端已在 Linux 實跑全套**（R 4.3.3、jmvcore 28.3、ellmer **0.2.0**、httr2 1.1.2）：**1388 pass / 0 fail / 0 error / 2 skip（live）**，含 module-catalog 全部、test-pii、test-eval-score（peer 修正版）。⇒ 兩條 ellmer 路徑（本機 0.4.x、cloud 0.2.0）皆綠。**GitHub Actions「tests」run 5（commit `4c7a5af`）已 ✅ success（2026-09-25 14:35Z）——依作者方案 A，請 peer fast-forward 併入 main。**
- （原記錄）module-catalog fixture 紅燈測試：修 fixture 或加 skip（CI 需綠才併 main）。
  - `test-module-catalog.R:176` `default dirs resolve relative env paths` — fixture `tests/testthat/fixtures/modules/env-home/rel` 未在 repo
  - `test-module-catalog.R:201` `self-location derives modules root` — fixture `tests/testthat/fixtures/modules/self-location/modules/…` 未在 repo
  - 兩者皆 `normalizePath(..., mustWork=TRUE)` 對不存在的 fixture 目錄失敗；既有問題（1.3.x 起即紅），非 v1.4 造成。
- 下一項：D（後記檢查表）→ B'（Submit 改 ActionButton，需真機 spike）→ 想法 (1) 路徑圖 M0 spike 項目列表。

## 推送 4：v1.4-D「Before you report」後記檢查表 + 摘要偏態

**改動**
- `R/askllm.b.R`：新增 `.ASKLLM_CHECKLIST_SUFFIX[[lang]]`（en/zh；四項：前提檢驗與 jamovi 位置、效果量與 CI、缺失值處理、樣本數是否足夠；檢定力模組只在提供清單中逐字出現時才可提）；`.askllm_system_prompt(..., checklist = FALSE)` 新參數，TRUE 時附加於 catalog 約束句之後、雙向邊界句之前（預設 FALSE → 既有輸出逐字不變）。`.askllm_build_payload(..., checklist = FALSE)`：TRUE 時追加 `checklist` 欄（格式 v1.5；FALSE 時與 v1.4 逐字相同）。`.runInner()` 以 `opt$addChecklist` 驅動，預覽與真呼叫兩處 system prompt 都帶。
- `jamovi/askllm.a.yaml`：新增 `addChecklist` Bool（default **TRUE**）；u.yaml 在 previewPayload 前加 CheckBox。R code tutor 不加。
- `R/data-summary.R`：`.skewness()`（m3/m2^1.5，n<3 或零變異 → NA）；numeric 段尾加 `, skew: x`（所有數值變項摘要格式微變；既有測試皆 grepl，不受影響）。
- `tools/run-golden.R`：新增 `checklist = TRUE` 參數（與 UI 預設一致）並寫入報告／json。
- README 兩語版 Quick start 加說明段。
- 測試：`tests/testthat/test-checklist.R`（新，離線）：預設 FALSE 逐字回歸鎖（3 role × 2 lang × has_catalog）、TRUE 時附加與順序、內容關鍵字、custom prompt 仍附加、payload v1.5、skew 數值與 NA 情境、a.yaml/u.yaml、原始碼掃描。
- **cloud 端 Linux 實跑全套：1459 pass / 0 fail / 2 skip。**

**本機驗證清單**
1. `jmvtools::prepare()` 重生 `askllm.h.R`（新增 addChecklist）；`devtools::test()` 預期全綠。
2. **golden 對照基線（作者指示）**：`pkgload::load_all(); source('tools/run-golden.R'); run_golden(provider='gemini')`（預設 `checklist=TRUE`），與基線 96.2（`golden-gemini-gemini-flash-latest-20260925-215520`）比較：
   - 期待：總分不低於基線；`guider-plantgrowth-assumptions` 應維持 100；各 Guider 題回覆應多出「Before you report」段。
   - 請另跑一次 `run_golden(provider='gemini', checklist=FALSE)` 作 A/B（同日同模型），把兩次 mean score 與差異題目貼回本節。
   - 回覆變長：留意 `max_tokens=4096` 是否有截斷（報告中回覆是否以完整句結尾）。
3. GUI：Module Guider 預設勾選 addChecklist → 回覆結尾出現「Before you report」；取消 → 重新 Submit 觸發新呼叫（非 cached）且無該段；再勾回 → 再觸發新呼叫（payload 指紋含 checklist）。
4. GUI：預覽面板的 system prompt 含檢查表句（addChecklist 開）／不含（關）。

**本機驗證（peer 填寫）**
- [ ] 1  - [ ] 2（checklist=TRUE mean = ？；checklist=FALSE mean = ？；差異題：）  - [ ] 3  - [ ] 4

---
